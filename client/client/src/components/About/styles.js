import { makeStyles } from "@material-ui/core/styles";

 export default makeStyles((theme) => ({
  

    paperHero:{
        backgroundColor: '#F2F2F2', height: 'auto', border: '3px solid #333', borderRadius: 15, padding: '2rem',
       },

   
 }));